// Ambient declarations for packages that lack TypeScript definitions.
// ffmpeg-static and ffprobe-static are loaded via createRequire in config.ts
// to avoid ESM/CJS interop issues with NodeNext module resolution.

